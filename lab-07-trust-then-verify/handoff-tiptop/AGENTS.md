# House rules for agents working on TipTop

## Always

- Money is whole cents; shares must sum to the exact grand total.
- Run the checks (`node checks/checks.js`) before reporting anything as done.
- Keep it a single file — no dependencies, no build step.

## Never

- Never add accounts, sign-in, or storage of any kind.
- Never claim a fix without naming the check that proves it.
