# Decisions — my delta log

<!--
This file is the record of your judgement: every time you overrode, redirected, or rejected what
the agent produced, one short entry. Together with your commit history, it's the proof that a human
ran this project — the "delta" in spec → delta → critique.

Write entries as you go, not at the end. Three sentences is plenty. The format:

## <date> — <what happened, in a few words>
The agent <what it did/proposed>. I <kept it / changed it to X / rejected it> because <the actual
reason — the "because" is the whole point of this file>.

A good entry from a real lab:

## 2026-08-12 — Rejected the sign-up screen
The agent added email sign-up "so users can save their splits". I had it removed because the spec
says one-off use at a table — nobody creates an account mid-dinner. Added "no accounts" to Must NOT
so it stays dead.
-->
